
    <div class="portlet light bordered">
        {{ form_start(form, {attr: {class: 'form-horizontal'}}) }}
        {{ form_widget(form) }}
        {{ update() }}
        {{ form_end(form) }}
    </div>

